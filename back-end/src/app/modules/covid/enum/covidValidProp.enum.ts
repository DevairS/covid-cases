export enum enCovidValidProp {
  date = 'date',
  variant = 'variant',
  location = 'location',
  num_sequences = 'num_sequences',
  perc_sequences = 'perc_sequences',
  num_sequences_total = 'num_sequences_total',
}
